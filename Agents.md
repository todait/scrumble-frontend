# Agents Guide

- 프론트엔드는 페이지 컴포넌트와 커스텀 훅으로 역할을 나눈다. 데이터를 가져오거나 실시간 동작을 다룰 때는 훅에서 처리하고, 페이지/컴포넌트는 표시와 상호작용에 집중한다.
- 실시간 기능은 `src/shared/services/centrifugo.service.ts`와 `src/shared/hooks`에 이미 구축된 계층을 확장한다. 새로운 웹소켓 연결을 따로 열지 말고 기존 훅에 기능을 추가한다.
- 테스트는 유닛/통합 원칙을 따른다. 가능하면 테스트 DB를 붙인 통합 테스트로 애플리케이션 훅과 리포지터리를 검증한다.
- 커밋을 만들기 전에 `npm run build`를 실행해 타입/린트 오류가 없는지 확인하고, 발견된 경고도 가능하면 함께 정리한다.
- 문서 업데이트 시 `instructions.md`를 우선 수정하고, 필요한 경우 여기에 에이전트용 메모를 추가한다.
