⏺ 현재 프로젝트 개선점 요약

1. 테스트 설정 미구성

   - Jest와 Testing Library 설치되어 있지만 jest.config.js 없음
   - 프로젝트 테스트 파일 전무

2. 환경 변수 문서화 부족

   - .env.example 파일이 비어있음 (HOST=만 존재)
   - CLAUDE.md에 명시된 환경 변수들이 .env.example에 반영 안됨

3. 코드 품질 이슈

   - ESLint 에러 7개, 경고 1개 존재
   - 사용하지 않는 변수들과 import 정리 필요
   - React Hook 의존성 배열 누락

4. 보안 취약점

   - localStorage에 사용자 정보 직접 저장 (XSS 취약)
   - 토큰 갱신 로직의 에러 처리 미흡

5. 타입 안정성

   - API 응답 타입 정의 부재
   - 에러 타입 정의 미흡

6. PWA 설정 미구성
   ㅎㅎ

   - next-pwa 설치되어 있지만 설정 없음
   - manifest.json, service worker 설정 필요

7. CI/CD 파이프라인 부재

   - GitHub Actions 등 자동화 설정 없음
   - 빌드/테스트 자동화 필요

8. 성능 최적화 부족

   - 이미지 최적화 미적용
   - 번들 사이즈 분석 도구 없음
   - 동적 import 미활용

9. 접근성 개선 필요

   - ARIA 레이블 부족
   - 키보드 내비게이션 미구현

10. 개발자 경험 향상
    - Prettier 설정은 있지만 .prettierrc 파일 없음
    - Git hooks (Husky) 설정 없음
    - 커밋 메시지 컨벤션 없음
